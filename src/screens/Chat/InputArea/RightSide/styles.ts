import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  rootArea: {
    paddingBottom: 14,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'flex-end',
  },
});
